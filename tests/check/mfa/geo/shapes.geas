// The dotted path module: import geo.shapes resolves here, one directory
// down from the root file, and the sum's variants become global
// constructors in the merged module.

Shape is either Circle(r: Int) or Square(side: Int)
